mixin loginForm()
    form.form(action="/login",method="POST")
        
        label(for="email") Email Address
        input(type="email", name="email")
        label(for="password") Password
        input(type="password", name="password")
        input.button(type="button", value="Log In")